import { render, screen } from '@testing-library/react';
import Filter from './Filter';
import { ThemeProvider } from 'styled-components';
import theme from 'theme';

it('render Filter', () => {
  // render(<ThemeProvider theme={theme}><Filter /></ThemeProvider>);
});